[package]
name = "lifecycle"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
memory = { path = "../memory" }
observe = { path = "../observe" }
scheduler = { path = "../scheduler" }
sensory-bridge = { path = "../sensory-bridge" }
