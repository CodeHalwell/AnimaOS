[package]
name = "tool-cache"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Tool response caching with TTL expiry and deduplication for AnimaOS — Epic E26."

[dependencies]
praxis = { path = "../praxis" }

[dev-dependencies]
