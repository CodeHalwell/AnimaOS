[package]
name = "feedback"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Response quality tracking and user feedback collection for AnimaOS — Epic E24."

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = "3"
