[package]
name = "metrics"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Prometheus metrics export and observability for AnimaOS — Epic E21."

[dependencies]
vita = { path = "../vita" }

[dev-dependencies]
vita = { path = "../vita", features = ["std"] }
