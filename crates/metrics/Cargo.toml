[package]
name = "metrics"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "E18 — Metrics & Observability: audit-log aggregation, Prometheus export, and periodic snapshots."

[dependencies]
vita = { path = "../vita" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = "3"
