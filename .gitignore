target/
xtask/target/
__pycache__/

# CycloneDX SBOM artefacts produced by `cargo cyclonedx` (CI generates these
# per-crate; local runs should not commit the output).
bom.cdx.json
bom.cdx.xml
sbom-out/

# QEMU FAT ESP image staged when booting the microVM kernel locally
# (esp/EFI/BOOT/BOOTX64.EFI + OVMF NvVars). Throwaway boot artefacts.
esp/
