[package]
name = "llm-backends"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
scheduler = { path = "../crates/scheduler" }
serde = { workspace = true }
serde_json = { workspace = true }
# Synchronous HTTP client for the Ollama backend. Default features ship
# rustls + json; no OpenSSL build dependency, so the container build stays
# clean.
ureq = "3"
